{
  "name": "exercise1",
  "version": "1.0.0",
  "description": "exercise 1 hello world",
  "main": "index.js",
  "scripts": {
    "watch": "babel src --presets react,es2015 --out-dir static --watch --verbose",
    "compile": "babel src --presets react,es2015 --out-dir static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "muktha",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  }
}
